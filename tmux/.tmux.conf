#set prefix to backtick
set -g prefix `
unbind C-b
bind ` send-prefix

# Enable mouse for scrolling and clicking
set -g mouse on

set -g update-environment "DISPLAY SSH_ASKPASS SSH_AUTH_SOCK SSH_AGENT_PID SSH_CONNECTION WINDOWID XAUTHORITY VSCODE_IPC_HOOK_CLI"